const fs = require('fs')
const MongoClient = require("mongodb").MongoClient
const mongoose = require('mongoose')


let controller = {
    queryDatabase: function (req, res) {

    },
    updateDatabase: function (req, res) {
    
    }
}
module.exports = controller